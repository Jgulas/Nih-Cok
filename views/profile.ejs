<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Profil Saya</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        background-color: #fdf6f0;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
      }

      .container {
        width: 90%;
        max-width: 600px;
        margin: 40px auto;
        background-color: #fff3e6;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      h2 {
        text-align: center;
        color: #5c3210;
      }

      p {
        color: #333;
        margin: 10px 0;
      }

      em {
        color: #888;
      }

      .button-container {
        text-align: center;
        margin-top: 20px;
      }

      .button-container a {
        text-decoration: none;
        background-color: #8b4513;
        color: white;
        padding: 10px 15px;
        border-radius: 6px;
        display: inline-block;
      }

      .button-container a:hover {
        background-color: #5c3210;
      }
    </style>
  </head>
  <body>
    <%- include('./partials/nav') %>

    <div class="container">
      <h2>Profil Saya</h2>

      <% if (!user.Profile) { %>
        <p><em>Profil belum dibuat. Silakan buat terlebih dahulu.</em></p>
        <div class="button-container">
          <a href="/profile/add">➕ Buat Profil</a>
        </div>
      <% } else { %>
        <p><strong>Nama:</strong> <%= user.name %></p>
        <p><strong>Role:</strong> <%= user.role %></p>
        <p><strong>Email:</strong> <%= user.Profile.email %></p>
        <p><strong>No HP:</strong> <%= user.Profile.phone %></p>
        <p><strong>Tanggal Lahir:</strong> <%= user.Profile.birthDate %></p>

        <div class="button-container">
          <a href="/profile/edit">✏️ Edit Profil</a>
        </div>
      <% } %>
    </div>
  </body>
</html>
